# Isolated initial design contract

You are an actual Codex design subagent in `codex_session` mode. Read only this contract and the specified task's `round3/evidence/<task>/` bundle. Do not open parent source code, expert/reward implementations, other task reports, D5/D10/D20 trajectories, or concrete development/test states. If a required piece of evidence is missing, ask the coordinating agent to complete the bundle. You may use Python through Pixi to summarize the two bundled demonstrations, and must view all 16 uniformly sampled frames. Do not claim visual-policy generalization: learned policies receive the shared numerical state schema only.

Propose exactly three meaningfully distinct candidates P1/P2/P3, based on task evidence and explicitly sourced geometry knowledge. No candidate mechanism is prescribed. Seriously consider a combination and record why adopted or rejected. Retain uncertainty and failure conditions, and do not presume priors must help. Do not implement or train; first save the proposal so the implementation can be audited against it.

All candidates retain diffusion action learning. Shared defaults: two observations, predict16 execute4; conditional U-Net widths64/128/256, time embedding128, kernel5, groups8; total batch128 chunks/update; total20,000 AdamW updates across the entire system, lr1e-4, weight decay1e-6, betas.9/.999, gradient clip1, EMA.995; epsilon prediction, 100-step cosine DDPM, DDIM16 eta0, no clean-sample clipping. World xyz+gripper action cube clipping happens after decoding. Trainable parameters at most3x raw-input B0 (~4.7 million for B0). Every N in2/5/10/20 trains from scratch seed0, using only its nested D_N for statistics, labels and parameter fitting. Initial designs cannot be changed based on additional demonstrations or model scores. One N5 development feedback revision is possible later, separately recorded and trained only N5/N20.

You may choose input/output representations, relation encodings, auxiliary learning, learned selectors/termination, parameter sharing or combinations as supported by evidence. At runtime use only declared common observations. No future states, expert actions, hidden simulator phase/success oracle, analytic expert action controller, rescue, test action search, or undeclared channel. Future states inside the training D_N can generate supervised labels. Episode clocks never restart at route changes. Explicitly define all coordinate axes, units, losses (target, scale, weight, application condition), label rules, schedules, runtime decisions, and chunk frame handling. Fixed robot, gravity and obstacles limit physical symmetry; do not presume object rotation/translation is valid whole-environment augmentation.

Implementation interfaces available for any actual proposal: `observation(raw)->encoded`, `action_encode(world_chunk, current_raw)`, `action_decode(encoded_chunk,current_raw)`, `supervision(current_DN,window_index)`, `build_policy(config)`, `extra_loss(model_output,batch,update)`, and `diagnostics(raw_history,model)`. Model architecture is open within the stated budget; the implementation is not restricted to a predefined prior library. These function names specify responsibilities, not a prescribed mechanism. The raw common schema is frozen before your proposal.

Write `round3/design_records/<task>/proposal.json` with top-level task, version=1, created_at (real UTC), agent_backend="codex_session", designer (actual subagent name; exact model/session/tokens/cost unavailable unless exposed), context_exposure (actual reading scope), evidence_hashes mapping repository-relative bundle files to SHA256, combination_consideration, and candidates in P1/P2/P3 order. Each candidate must contain:

`candidate_id, knowledge_hypotheses, evidence_refs, predicted_generalization_gain, applicability, failure_conditions, architecture, modules, observation_transform, action_transform, losses_and_weights, augmentation, training_schedule, hierarchy_selection_and_termination, chunk_transition_handling, required_train_labels, required_runtime_inputs, parameter_budget, implementation_steps, confidence`.

Each knowledge hypothesis labels its source as external geometry/common knowledge, observation from D2, or unverified inference. References identify specific bundle files, episodes and times where useful. Predictions are prospective qualitative expectations, never invented outcomes. Also save concise `rationale.md` describing design choices and limits. Save the actual original design before any implementation; a substantive feasibility revision gets a new version and reason and may happen at most once.
