# 开发规则

本文件约束新增接口和后续开发；历史实验按 Experiment 1 最新授权归档，保留其源码和记录内容。

- **No ad hoc**：不添加临时拼凑、针对个别样本或结果的特殊逻辑。
- **No heuristic**：不新增启发式规则，不猜测观测、动作、硬件语义或缺失参数。
- **No error handling**：不添加静默异常恢复或自动重试。明确的契约诊断、用户要求的受控工具修复、终止失败记录和精确中断恢复须公开记账，不得掩盖失败或变成性能搜索。
- **No fallbacks**：不提供备用模型、备用后端、默认伪数据或静默替代行为。
- **Always use Pixi**：所有 Python 和项目命令必须通过 `/home/users/oscar/.pixi/bin/pixi run ...` 执行；Experiment 1 使用锁定的默认 MetaWorld 环境。不创建 Conda/venv，不使用裸 pip。
- 允许按实际需求修改 Pixi 配置，但须保留历史依赖与来源记录。新增接口优先复用现有环境。
- 按用户 2026-09-13 最新分配，训练、测试和渲染可使用全部物理 GPU **0–7**；调度前检查实际占用，保留所有无关进程。资源变更单独记录，原协议与历史记录保留当时真实卡号。
- 无法获得准确接入信息时，保留空的抽象接口；不模拟已经接通的硬件或 API。
- 保持先验提取的证据、方案冻结、实现审计、训练、开发选择和锁定测试流程不变。
- 保留冻结数据、配置、历史源码和运行产物；训练、评估、API 或硬件接通的完成声明必须有实际验证证据。

- Experiment 1 的 B1 是用户明确要求、在 API 设计前冻结的公共规则 baseline。A1–A4 的具体归纳偏置和实现由 RuntimePriorAPI 工具会话产生，开发 Codex 不得代写或替换。
- API 只编辑其未提交候选；初始三候选全部提交后才提供开发反馈，仅 A4 可据此设计。公共训练器、评估器、划分和成功判定只读。
