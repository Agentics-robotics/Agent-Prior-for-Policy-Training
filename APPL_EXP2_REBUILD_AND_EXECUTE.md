# APPL：代码整理、GPU 神经训练接通、DP 排错与 Exp2 重做指令

这是一份交给仓库内 Codex / coding agent 的执行指令。请直接检查仓库、修改代码、验证并运行实验，不要只回复方案，也不要把任务缩减为改文档或生成一套未经运行的脚手架。

## 0. 任务目标与执行顺序

项目是 APPL（Agentic Prior for Policy Learning）。我们希望验证：在有限完整示范下，Agent 可以分解任务，为神经子策略提出并实现合适的表示、loss、结构或 inductive bias，再在长程执行中调用这些子策略，改善运动泛化及任务组合能力。

本次必须按以下顺序推进：

1. **整理仓库。** Exp1 独立保存、保持可复现；Exp2 收敛到唯一清晰入口，消除散落的旧实现、恢复脚本和重复配置。
2. **先验证代码和控制链路。** 检查示范、观测动作对齐、控制器、训练与评估实现；先通过必要的功能检查，再做小规模诊断训练，禁止直接启动大规模矩阵。
3. **接通真实神经训练。** 复用并核实 Exp1 已有的 API / GPU 工作方式，让 API 能提交实际可训练的 PyTorch 候选，修改表示、loss、结构及 prior；由训练工具在允许的 GPU 上执行。
4. **把 DP 的 ID 失败查清楚。** 从动作回放、单轨迹拟合到新 ID 初始状态逐级检查。低训练 loss、checkpoint 存在和接口无异常都不能代替闭环成功。
5. **先做同一 backbone 的 prior 对照。** 除完整轨迹 DP 外，加入同样分段、同样高层调度、但不使用 Agent prior 的子策略基线。
6. **再开展 ID / OOD 正式实验。** OOD 可包括位置外推、未见位置组合，以及部分子目标已完成的任务状态变化。你可以根据实际任务自主设计合理、可达的设置。
7. **产出真实结论。** 研究假设是“DP 能学会 ID，但 OOD 受限；APPL 在保持 ID 的同时改善 OOD”。这是待检验的假设，不是必须拼出来的结果。

用户授权你做必要的重构、修复、无效运行清理、重新训练和评估。常规实现选择自行完成。遇到真实的权限或资源边界再明确说明；不要每完成一个阶段就停下来问是否继续。

## 1. 已知背景：上一轮到底哪里没有落实

以下来自 `REVIEW_REPORT.md` 和 `REPORT.md`，用于定位问题，必须再核实当前源码。不要把报告中的历史状态当作当前仓库已验证事实。

### 1.1 原任务和数据

- Panda 打开抽屉，将红块放到抽屉外的标记垫，将蓝块放入抽屉；抽屉保持打开。
- 原始数据为 15 条完整 ManiSkill 示范，16006 个动作、16021 帧观测。切成 45 个片段不等于得到 45 条独立示范。
- 原控制为 20 Hz，7 个 Panda 绝对关节位置命令加 1 个夹爪命令。当前代码中的动作归一化、裁剪和具体控制器配置仍需实查。
- 原 OOD 是蓝块一开始已在抽屉中。它主要检查是否处理已完成子目标，不足以代表一般位置泛化或任务重排。

### 1.2 APPL 侧没有进行神经网络训练

- 旧候选仅能使用 math、NumPy、appl_public 和候选自身模块，未接通 Torch/GPU 后端。
- API 提交的是示范检索、按时间回放绝对关节轨迹，以及部分近邻 IK 位置修正。
- 每次训练入口运行 100 次 update，但只有第一次构建示范记忆，后续 99 次无实际更新。
- 两轮共 6 次训练尝试，只产生 3 种 checkpoint 内容；同技能第二轮与第一轮 checkpoint hash 相同。
- 第二轮修改配置和技能结束条件，没有学习新的神经权重。问题包含框架能力缺失，不能归咎于 API 不愿训练。

### 1.3 DP 确实训练了，但完整任务失败

- 完整轨迹状态输入 DP：2 帧观测、16 步预测、执行前 8 步。
- 条件 U-Net，通道 128/256/512，约 1691 万参数。
- 100 步 DDPM 训练噪声调度、16 步 DDIM 推理；AdamW，学习率 1e-4，batch 128，EMA 0.999，seed 0，12000 次梯度更新。
- 报告训练耗时约 323 秒。不能仅凭耗时短或更新数判断训练真假、充分性或正确性。
- 两种方法均为 ID 0/4、OOD 0/5；各另有一个 ID 运行因基础设施中断，最终成功未知。
- DP 在完整 ID 案例中曾打开抽屉，但红块未到垫区，最终抽屉重新关闭。

### 1.4 控制和验证层也有明确问题

- 红块技能可能在接近垫子时提前返回完成，省略松爪、稳定和退出，之后切换技能使红块离开目标。
- API 宣布完成不能代替固定评估器判断。
- OOD 的蓝块技能是“被调用后前置检查返回 0 步”，不能写成高层 Agent 主动跳过技能。
- MuJoCo 红块重放 RMSE 约 17.7 cm，完整组合验证 4 次均失败；该模型不能直接视作可信接触仿真器。
- 只有 `env.step` 推进物理时间；等待 API 或再次 observe 不会让物体自动稳定。
- 旧执行路径有大量逐步 RPC，APPL 端到端耗时约为模拟时间 6.72 倍；不要把低层每一步都交给远程模型。
- 历史上有磁盘满和意外触及非指定 GPU 图形上下文的问题，新启动器必须解决。

## 2. 先整理仓库，避免再叠加一个 round3 / final_fix 副本

### 2.1 先检查当前工作区

优先读取适用的 `AGENTS.md`、根 README、Pixi 配置，以及 Exp1、Exp2 当前入口。用 `git status`、`rg --files` 和有针对性的 `rg` 定位文件；不要输出密钥。

历史路径线索包括：

- `/home/users/oscar/agent_learning/Agent-Prior-for-Policy-Training`
- `frameworks/appl/round2`
- `/home/storage/oscar/appl_round2_20260914`

这些只是线索。先核对当前路径、符号链接和实际文件位置，不要直接在历史绝对路径上批量移动或删除。

检查：

- Exp1 的可运行代码、API 客户端、GPU 训练入口、数据、配置、已有结果和锁文件分别在哪里。
- Exp2 的训练、部署、环境、API 工具、恢复脚本分别由哪个入口真正调用。
- 哪些是有效实现，哪些是旧副本、调试产物、生成缓存或被废弃的包装层。
- 哪些文件有用户未提交修改；先保留，不要覆盖或用 reset/clean 清空工作区。
- 现有磁盘空间、训练输出实际挂载点和允许使用的 GPU。不得因为某卡空闲就默认获得权限。

### 2.2 建立唯一的目录职责

下面是推荐布局，允许为兼容现有仓库作小幅调整。关键是职责清晰、只有一套有效实现，而非强行照抄路径。

| 位置 | 职责 |
| --- | --- |
| `experiments/exp1/` | Exp1 的独立入口、配置、说明和结果索引；保留原实验行为 |
| `experiments/exp2/` | Exp2 的配置、API prompts、协议、文档和最终报告 |
| `src/appl/` | 经确认可复用的核心：数据接口、策略接口、训练器、Agent 工具、评估 |
| `tests/` | 覆盖已识别的控制、数据、训练和退出条件风险的必要测试 |
| `data/` | 只读数据及 manifest；大文件通过唯一受管理路径引用 |
| `runs/exp1/`、`runs/exp2/` | 各自的运行产物，底层可映射到有空间的存储盘 |
| `archive/legacy_exp2/` | 旧 Exp2 源码和简要说明；不进入正式 import 和运行入口 |
| 根 `README.md`、Pixi 配置 | 环境及统一命令入口 |

要求：

1. Exp1 单独存好。迁移后保留数据、checkpoint、配置及结果的内容一致性，验证原入口或兼容入口仍可运行。
2. 不为了 Exp2 顺便修改 Exp1 算法、报告数字或重新训练 Exp1。若共享依赖会破坏 Exp1，保留 Exp1 的独立锁定环境，不强制合并。
3. Exp2 只保留一个正式训练入口、一个评估入口和一个配置解析路径。恢复逻辑集成进去，不再复制整份 runner 打补丁。
4. 不大范围复制原始数据和 checkpoint。处理符号链接前核对目标，避免递归移动、重复占空间或误删真实数据。
5. 数据、运行产物、源码分开；不将大 checkpoint、token 日志、视频和缓存提交到 Git。
6. 不搭建与本次实验无关的通用平台。保留清晰的公共模块，删除已确认无用的重复封装。
7. 写一份简短 `experiments/exp2/MIGRATION.md`：旧路径 → 新路径、保留项、归档项，以及新命令。

沿用 Pixi。不要另建 Conda 环境。复用已存在且适用的锁；必要的依赖修改在开发阶段显式更新并验证，正式运行使用锁定环境。

### 2.3 提供可执行的统一命令

例如实现以下真实可用的 CLI；名称可调整，但 README 必须给出实际验证过的命令：

```bash
pixi run --locked exp2 audit
pixi run --locked exp2 check
pixi run --locked exp2 diagnose-dp --config experiments/exp2/configs/diagnostic.yaml
pixi run --locked exp2 design --config experiments/exp2/configs/main.yaml
pixi run --locked exp2 train --config experiments/exp2/configs/main.yaml
pixi run --locked exp2 evaluate --config experiments/exp2/configs/main.yaml
pixi run --locked exp2 report --config experiments/exp2/configs/main.yaml
```

命令可以分步执行和恢复，并从同一配置解析设备、数据、模型与输出路径。不要把未实现的示例命令写成可运行入口。

## 3. 先通过正确性检查，再花时间训练

按依赖顺序检查。使用能真正发现错误的少量测试和端到端检查；不要只写 mock 测试或检查文件存在。

### 3.1 数据与控制契约

明确记录以下映射，并在数据加载、训练、推理、环境四处核对：

- `observation_t` 是否对应执行 `action_t` 之前的状态，而非执行后的状态。
- 关节、夹爪、物体、目标位置、TCP 字段的顺序、维度、单位和坐标系。
- action 是绝对关节位置、关节增量、归一化控制命令还是其他语义；实际 wrapper 是否又做了一次转换。
- 夹爪正负方向、开闭范围、宽度与命令之间的映射。
- 训练动作、反归一化后的输出、环境接收的动作三者是否一致。
- 控制频率、模拟频率、重复步数、动作 chunk 的时间跨度是否一致。
- quaternion 顺序、角度单位、机器人根坐标与世界坐标是否一致。
- episode 边界、history padding、末端 action padding 和 loss mask 是否正确。
- 归一化统计是否只来自训练数据；是否对不同量纲错误混算、对低方差维度数值不稳定、对 OOD 输入错误裁剪。
- 训练与验证按完整轨迹划分，禁止把同一示范相邻窗口分到训练和验证后声称是独立泛化。

保存少量可读样例：原始观测动作、归一化值、反归一化值、送入控制器的最终动作。不要转储整份数据。

### 3.2 原生环境示范回放

在示范来源的原生环境中，从可复现的初始状态回放保存动作，用正式评估器检查是否完成任务。

- 优先使用完整原生 simulator state；仅恢复物体位置、把速度清零，不等于完整状态恢复。
- 如果旧数据缺少完整 reset state，明确标记，使用已知可复现的原生 seed/reset 流程验证；必要时补采少量诊断示范，不要把近似复位失败误判成动作文件损坏。
- 至少覆盖几条完整示范，检查开抽屉、抓取、搬运、释放、稳定、终止。
- 若原生示范回放都失败，先修复数据/控制/复位，不启动正式神经训练。
- 如果只有“物体曾经过目标上方”的视频，不能认为示范已成功。

### 3.3 评估器与技能退出

在新协议正式运行前，把成功条件定义完整并固定，对所有方法一致使用：

- 抽屉开启阈值。
- 红块位于有效垫区，蓝块位于抽屉有效区域；明确使用中心点还是物体占据范围。
- 物体速度低于合理阈值并持续规定物理时间。
- 完成放置时夹爪已释放、物体不依赖机械臂夹持维持目标状态。
- 不允许技能自行重定义环境最终成功。

策略工具区分 `already_satisfied`、`succeeded`、`timeout`、`failed`、`infrastructure_error`。时间耗尽或回放结束不能自动返回成功。

技能交接需要真实完成末尾动作，不能只检查“物体进入 XY 区域”。定义下一技能可接手的手臂/夹爪条件，或让训练覆盖真实交接状态。

若定义统一的结束确认窗口，必须预先固定动作规则、时长和适用条件，计入步骤及时间，所有方法使用相同规则。禁止只给 APPL 添加一个隐藏的成功修复控制器。

## 4. 接通 API 的 GPU 神经训练能力

### 4.1 复用 Exp1 的有效实现

读取 Exp1 实际的 API 工具循环、候选代码入口、GPU job 启动方式、checkpoint 保存和加载流程。核实它确实执行梯度更新，而非只复用名称。

API 是设计者与工具调用者；训练器负责执行 GPU job。不要解释为模型服务本身直接占用本地 GPU，也不要因为旧 NumPy 沙箱限制就退回示范回放。

API 至少能完成：

1. 读取公开训练数据说明、少量轨迹及可用状态字段。
2. 提出并提交技能分段、候选代码、配置、prior 说明及适用条件。
3. 编辑神经候选的表示/编码、loss、结构和 prior 实现。
4. 运行接口检查及小规模真实 GPU 训练检查。
5. 提交有足够预算的完整训练作业，查看进度、训练诊断和验证反馈。
6. 在一次反馈修订中保留/删除候选、修改机制或 loss，并重训受影响的子策略。
7. 冻结最终技能库，部署阶段只观察、选技能、调用及结束。

模型和 API 接入优先复用用户现有配置与 Exp1 的可用设置，不擅自换成便宜模型、模拟 API 响应或让本地 Codex 代写最终候选后冒充 API 设计。

### 4.2 候选接口要允许实质性修改

使用简洁的代码接口，而非只让 API 从几个 prior 名称中选项。例如可以提供：

```python
build_model(spec)
encode_observation(batch, spec)
compute_loss(model, batch, spec)
predict_action(model, observation_history, spec)
skill_precondition(observation, spec)
skill_postcondition(observation_history, spec)
```

具体签名由代码决定，但必须满足训练和部署一致、可保存重载、输入输出可校验。框架负责设备、优化器执行、数据权限、作业生命周期和最终评估，API 负责候选机制。

可修改的内容包括：

- 表示：物体相对坐标、目标相对坐标、角色选择、适用字段掩码、历史编码。
- Loss：辅助几何约束、阶段条件项、一致性/等变性约束、合理的权重或训练采样设计。
- Prior：对称性、局部参考系、接触阶段、目标关系、状态适用条件等。
- 结构：编码器、条件注入、模块组合；主对照中受同 backbone 约束，结构探索作为后续单独实验。

**开放这几种修改能力，不代表强迫 API 在每个技能上全部使用。** 允许它判断某 prior 不适用；必须记录实际选择及理由。

对表示变换要特别检查动作端。绝对关节动作不会因为改了观测坐标就自动获得平移/旋转等变性。需要明确、可执行的输出解码或约束，并验证能回到同一原生控制接口。任何额外 IK、规划器或控制器贡献应单独说明，不能默认为表示 prior 的收益。

### 4.3 必须有“真的训练了”的证据

每个神经候选至少记录：

- 模型类、可训练参数数量、数据片段与训练配置。
- 实际 GPU 设备、非零 optimizer step 数、batch size 和训练耗时。
- 初始与训练后权重差异；少量步骤的有限且非零梯度检查。
- Loss 曲线、验证结果、有效样本曝光量。
- checkpoint 重新加载后的输出一致性与可执行 rollout。

若无 trainable parameters、无有效梯度、所有更新都 no-op，立即报错；不能标记神经训练成功。

若第二轮声称重训但权重未变，查明是主动复用、不涉及权重的配置变化，还是训练故障；必须按实际情况记账。

### 4.4 预算与运行效率

- 移除旧的 100 update 固定限制。预算由真实学习曲线和诊断决定。
- 先小规模估计收敛区间，再配置正式预算；可从数万次有效梯度更新的量级探查，必要时增加，而不是机械认定 12000 或某个数字足够。
- 同时记录 update、batch、样本曝光量和 GPU 时间，避免用不同 batch 的相同 update 数冒充同等训练量。
- 在可用资源范围内继续训练到预定收敛/预算标准；不无限追加到目标测试成功为止。
- 子策略训练与加载后推理在本地工作进程执行。一次 `invoke_skill` 应能持续运行低层闭环；远程 API 只在高层决策或规定检查点调用。
- 使用允许的 GPU；在进程内核对物理卡映射及渲染上下文。所有 GPU 训练、渲染都经统一启动器，避免隐藏地触及其他卡。
- 不把 API 凭据放入候选进程，不放开与训练无关的网络或全盘写权限；通过受控 worker 开放所需 Torch/CUDA 和文件访问。
- 调度前检查磁盘/显存，合理保留 checkpoint（如 last、best 和必要的调试快照），不要默认保存每一步或大量重复模型。

## 5. 查清楚为什么 Naive DP 连 ID 都不能完成

DP 在合理条件下应有机会学会该 ID 任务，这是很合理的排错起点；但 15 条示范、长时程、接触误差和闭环分布偏移意味着成功没有数学保证。请逐级验证，不能直接断言“必然是 bug”，也不能把现有 0% 轻易归因于 DP 方法不行。

### 5.1 检查训练实现

重点核对：

- 训练窗口有没有跨 episode/技能边界错误取样，是否对齐了动作 chunk 和观测 history。
- U-Net 的时间、通道、条件维度及 noise timestep 编码是否正确。
- DDPM / DDIM 的 prediction type、噪声日程、推理时间步和输出尺度是否一致。
- 训练目标究竟是 noise、sample 还是 velocity；采样器有没有按错误目标解码。
- 每个 step 是否真实 backward / optimizer.step；EMA 是否更新并从正确 checkpoint 加载。
- train/eval 模式、随机种子、推理缓存和 history reset 是否正确。
- 在线历史是否混入上个 episode；动作队列是否执行了旧 chunk。
- 是否错误地把所有输出裁剪为 [-1, 1] 后当成绝对关节角，或重复反归一化。
- 夹爪目标是否被过度平滑、符号颠倒或错误归一化。
- 是否加载了随机初始化模型、错误数据任务的模型、错误 EMA key 或不匹配的 normalizer。
- 数据是否包含完整松爪/稳定阶段；固定 horizon 和 episode truncation 是否把必要末尾裁掉。

可以核对项目已有的官方 Diffusion Policy 实现及其锁定来源，但不要为解决一个局部问题盲目升级所有依赖。

### 5.2 诊断阶梯

| 阶段 | 做什么 | 如何解释失败 |
| --- | --- | --- |
| D0 | 原生初始状态回放示范动作 | 优先定位数据、控制、复位和任务评估 |
| D1 | 一个 batch 的训练拟合与正常采样 | 定位优化器、模型条件、目标、采样和尺度错误 |
| D2 | 对 1–3 条示范充分过拟合，从对应原生起点闭环执行 | 定位部署链路、动作时序及误差累积；不能只看训练 loss |
| D3 | 分别训练开抽屉、搬红块、搬蓝块的小型 DP，验证各技能 | 区分单技能学不会和长程/交接失败 |
| D4 | 用正式数据预算训练完整轨迹 DP，在未用于训练的 ID 开发种子评估 | 检查新 ID 初始状态的学习能力 |
| D5 | 固定配置、训练充分后，进入最终 ID / OOD 测试 | 测算法表现，不能再当作任意调参的调试场 |

D1 的含义是去噪目标及采样链路表现合理，不要求随机噪声训练 loss 精确变为零。

对 D2/D3，查看视频和结构化轨迹，标记第一次偏离发生在哪个阶段。不要仅输出最终 success=False。

### 5.3 优化 DP 必须合理，不能刻意削弱 baseline

按诊断需要检查或调整训练步数、学习率、normalization、动作预测/执行 horizon、history、数据覆盖和窗口采样。只使用训练集和 ID 开发集选择配置；记录尝试过的少量有针对性的设置。

建议先固定小型 pilot 的 ID 种子，例如 20 个独立 reset，以 **约 80% ID 完整成功率**作为主任务进入大规模对照的工程目标。该数字不是论文结论或成功承诺；先根据任务难度确定并记录，再运行，不要看完结果后降低门槛掩盖失败。

如果当前数据量不足：

1. 可以使用更多诊断示范检查“数据充足时是否可学”，但单独记为诊断实验。
2. 正式小数据对照中，所有方法必须回到相同数量的训练示范；不能只给 DP 或 APPL 偷偷加数据。
3. 如果任务确实过难，可在正式协议确定前简化任务或选择更合适的起点，并用相同新任务重新验证所有方法。保留原问题的简短解释。
4. 如果合理排错后仍然 ID 失败，准确报告剩余原因与证据。不要交付一张 baseline 全零的正式表就声称验证了 OOD 泛化。

形成简短 `DP_DIAGNOSIS.md`，区分：已确认 bug、训练不足/数据覆盖、长程闭环问题、尚未确认的猜测。每个修复配最小证据即可。

## 6. 实验设计：先隔离 prior，再比较整体系统

### 6.1 至少三个方法

| 方法 | 训练与部署 | 回答什么问题 |
| --- | --- | --- |
| M0：Full-trajectory DP | 完整轨迹训练一个神经 DP，无高层技能调度 | 标准端到端基线在 ID / OOD 如何表现 |
| M1：Hierarchical DP without Agent prior | 与 M2 相同分段、相同每技能 DP backbone、相同高层调度和技能接口，去掉 Agent 提出的表示/loss/prior 修改 | 分段与层级结构本身能带来多少收益 |
| M2：APPL with Agent prior | 在 M1 基础上启用 API 实际设计且已训练的 prior | 控制主要其他因素后，prior 是否有增益 |

若需要主张“Agent 比固定人工先验更会选择”，增加 M3：相同层级设置加一个预先固定的合理 rule-based/manual prior。仅 M1 vs M2 能说明所提出 prior 的作用，不能单独证明 Agent 优于人工选择。

主对照的要求：

- M1/M2 共享 API 确定后冻结的技能切分、技能数量和任务定义；不要让分段质量混入 prior 对照。
- M1/M2 使用同一高层调用机制、可用状态、公开目标几何、技能完成语义和恢复权限。
- 若为了 motion prior 的单独测试固定技能顺序，应对两者都固定；任务状态变化测试则对两者都启用相同的高层调度能力。
- 主对照固定 DP backbone 类型、主要宽度深度、扩散配置和输出控制接口。API 可以改表示、loss 或约束；由表示变化引起的小额参数差异要记录，尽量控制。
- 显著修改 backbone 或增加大编码器时，归入单独结构探索，不能并入“只改变 prior”的结果。
- 手工设计给所有方法的公共终止/释放逻辑要一致；若某项技能边界机制也是 Agent 方法的一部分，另列其消融，不能悄悄混进 prior 因果结论。
- M0 与多子策略系统在参数总量、调用成本上天然不同，必须列出。不要声称三者除了 prior 完全相同。

### 6.2 数据、信息与算力

- 优先沿用现有完整示范，先核实它们实际有效。清楚区分总示范数、训练示范数、验证示范数和片段数。
- 若已有数据只够做整轨迹训练/验证划分，明确实际训练条数；不能把 12 train + 3 val 写成 15 条训练示范。
- 同一个方法比较单元使用完全相同的原始训练示范、train/val 划分、状态字段和测试初始状态。
- API 可以从公开任务说明和训练示范中推断 prior；禁止获取隐藏测试 seed、最终结果或测试 rollout 来改候选。
- 首轮以状态输入为主，便于定位机制。不要一边给 APPL 额外 RGB 和物体/目标真值，一边给 baseline 缺失字段的输入后声称感知条件匹配。
- 目标位置发生变化时，所有需要完成目标的策略必须有相同的目标信息；不能让 baseline 输入里根本没有变化后的目标。
- Agent prior 自带的数据变换/增强须记录。与之无关的通用增强对照要一致，不能使用额外 OOD 示范冒充纯先验。
- 主因果对照重点匹配 M1/M2 每技能训练曝光量、backbone 和 checkpoint 选择规则。
- Agent 的候选探索、修订重训和 MuJoCo 成本额外记账；必要时提供计算预算匹配的对照，不要只比较最终模型一次训练的成本。

### 6.3 checkpoint 与统计规则提前固定

使用 ID 开发实验确定足够的训练日程和 checkpoint 选择规则，再冻结正式配置。正式所有方法使用相同类型的选择依据，例如预定训练步数的最终 EMA，或预先约定的共同开发集指标；禁止按最终 OOD 成功率挑 checkpoint。

先做小规模有效性检查，再扩展。资源允许时，主结果建议至少 3 个训练随机种子，每个 ID / OOD 条件每种子约 30 个配对环境 reset；这是初始建议，可按任务时间合理确定，必须在正式测试前写定。

共享设计出的 prior、重复 3 个训练 seed 只测训练随机性，不等于测了 3 次独立 Agent 设计。两者在报告中明确区分。

## 7. OOD 可以自主设计，但必须有物理意义

你可基于仓库实际任务选择最合适的设置。先保留一个范围清楚的 ID，再至少定义一种运动 OOD 和一种任务状态/组合 OOD；资源有限时不追求堆满所有类型。

| 条件 | 建议设置 | 主要检查 |
| --- | --- | --- |
| ID | 与训练同分布的新 reset、位置采样和随机种子 | 基础模仿学习能力 |
| OOD-position | 红/蓝物体或目标位置超出训练取值范围，但仍在可达空间内 | 相对表示、局部关系或其他运动 prior 是否有帮助 |
| OOD-combination | 各单独位置仍在训练范围内，但物体与目标的组合未在训练中共同出现 | 关系组合而非单纯位置外推 |
| OOD-state | 如蓝块已在抽屉内，或一个子目标已经正确完成 | 是否根据当前状态选择剩余技能 |
| 可选 OOD-orientation | 对有意义且可达的物体/目标方向进行未见变化 | 与所选 prior 对应的方向泛化 |

要求：

1. 写出数值化的训练范围、ID 分布、OOD 范围、坐标系与采样方式。不能只写“随机移动一点”。
2. OOD-position 的外推轴应明确超出训练支持；不要把训练范围内部的新随机 seed 当作位置 OOD。
3. OOD-combination 应确认边缘取值在训练范围内，改变的是联合组合；与位置外推分开报告。
4. 位置变化不能导致无法抓取、夹爪进不去、物体穿模、目标超出工作空间或任务条件互相矛盾。
5. 使用独立的参考控制/可达性检查验证任务可解；这些诊断动作不可混入正式训练数据。检查代表性状态，并设置统一的物理有效性采样条件。
6. 不能用 learned policy 的失败来定义“无效场景”再排除。物理不合法判据应独立于方法表现。
7. 可在开发阶段根据任务几何选择由易到难的多个难度档，再一次性冻结。不能看 DP 的最终结果后移动 OOD 范围，直到它失败。
8. 目标条件、观测权限、步数上限和成功条件对所有方法相同。
9. OOD-state 中分别记录高层是否根本没调用已完成技能、技能前置检查是否 0 步退出，以及是否仍产生了不必要动作。
10. 部分目标已完成不是任意任务重排。只有测试了新的有效顺序或组合，才能声称对应的组合能力。

如果 DP 在某些 OOD 也成功，完整报告，这是有信息的结果。如果 APPL 没有增益或损害 ID，同样报告并分析 prior 的适用边界。

## 8. MuJoCo 验证与“一次修订”的边界

保留用户想要的流程：API 设计第一版技能库，训练，调用 MuJoCo 验证，然后根据反馈最多修订一次；可保留/删除模型、修改 loss 或机制、重训受影响子策略，得到第二版最终库。

### 8.1 先让验证环境可信

- 明确原生任务环境和 API 使用的 MuJoCo 开发环境的角色，不能混写成同一个环境。
- 复用已确认的机器人模型、控制接口和可用公开观测；由 API 负责的场景建模或配置代码应有实际来源记录。
- 必须检查接触、摩擦、抽屉约束、抓取、松爪和物体稳定；只检查 TCP 误差不够。
- 定义与物体尺寸、夹爪余量、目标区容差相关的校准标准，并在 Agent 正式候选验证前固定。红块 17.7 cm 级误差不能被当作通过。
- 用训练/开发示范做单技能和完整组合复现，检查物体状态以及抓取/释放事件，而非只读技能返回码。
- 不能随候选表现任意调整物理参数使某策略通过。模型修复应有独立观测证据，并作为开发版本变更记录。
- 若代理仿真可信度仍未达标，先修复或明确标记该环节未完成，不得声称实现了有效的 sim-guided prior refinement。

### 8.2 正式 Agent 周期

严格区分：

1. 设计第一版 → 真实训练。
2. MuJoCo 开发验证 → API 读取反馈。
3. 一次库修订 → 修改/保留/删除候选，真实训练需要重训的模型。
4. 冻结第二版。允许预先规定的只读验证和结果检查，不再更新模型、代码、loss、配置或根据反馈选择新的部署超参数。
5. 最终 ID / OOD 评估。

若第二版只读验证失败，保留失败结果，不允许悄悄再来一轮候选修订。若发现的是可定位的框架 bug，按下一节的新实验版本规则处理，不冒充同一次合法修订。

开发阶段的框架排错不等同于正式 Agent 设计轮次。必须先完成可运行实现，再启动有固定预算的正式流程，避免把大量调试运行混入论文的“一轮修订”宣称。

## 9. 中途出错：修代码、清理无效产物、干净重启

用户希望代码整洁，不希望错误记录和恢复补丁到处堆积。按以下方式执行，不能因历史运行被冻结而拒绝建立正确的新实验。

### 9.1 确认属于实现或基础设施错误

例如动作映射错误、加载错误 checkpoint、训练没有更新、字段错位、GPU 错配、磁盘满、数据损坏或 evaluator bug：

1. 停止受影响的本项目 job，避免继续浪费训练和生成错误结果。
2. 定位根因，直接修复正式源码，不复制一个新 runner 临时绕过。
3. 添加或运行针对该错误的最小回归检查。
4. 在一个简短 incident 索引中记录 run ID、原因、受影响方法/条件、修复版本和重跑范围。
5. 从正式统计中移除失效运行；清理本项目相关无效 checkpoint、缓存、视频和冗余日志。保留定位问题所需的极少量证据即可，无需永久保存整套无效产物。
6. 用修复后的统一代码和一个新的 run ID 从干净初始状态重新执行受影响单元。

禁止删除原始示范、Exp1 有效产物、用户未提交代码或无法确认归属的目录。清理前按 run manifest 明确文件范围，检查符号链接真实目标，不对共享目录做宽泛删除。

### 9.2 策略真的失败，不属于可清除的程序错误

例如正确执行后抓空、松爪失败、无法处理 OOD、ID 成功率低：这些是有效结果，要保留在该实验中。可以在开发集上进一步诊断和改进，但不能删除失败 episode 并只补跑成功案例。

### 9.3 重启与版本规则

- 开发阶段：发现 bug 后自主修复、清理无效输出、重启，不必为普通修改反复确认。
- 正式阶段：若共享训练、控制或评估代码有 bug，明确影响范围，受影响的各方法和配对场景统一重跑，不能只重跑 APPL 的失败案例。
- 如果只有某训练作业的基础设施中断，修好后可从可靠训练 checkpoint 恢复；保证 optimizer、scheduler、EMA、RNG 和进度状态一致。
- 如果机器人 rollout 中断且没有完整 simulator snapshot，从新的 episode 起点重跑，标记原运行中断；不要将两段不同物理过程拼成一次成功轨迹。
- 已看到最终测试反馈后提出新的算法修改，必须建立新的开发周期和新的未使用测试集，不能继续称为原协议的一次修订。
- 原轮次报告可保留为简短历史记录，新正式结果只来自同一有效协议版本。代码无需永远沿用错误的旧冻结版。

## 10. 报告什么，才算完成研究验证

每个方法、每个 ID / OOD 条件至少报告：

- 完整任务成功数 / 有效评估数，以及单独列出的中断数量。
- 各子目标最终成功、释放/稳定状态，以及失败最先发生的阶段。
- 成功轨迹完成步数/模拟时间；失败轨迹的终止或超时情况单独报告，不能把提前失败当成更快。
- 端到端 wall-clock、高层 API 次数与等待时间、本地低层推理时延。
- 每个技能和总体参数量、训练梯度步数、训练曝光量、GPU 时间。
- Agent 候选数、实际重训数、一次修订前后变化及开发验证成本。
- 多训练 seed 的结果与不确定性；配对环境种子结果使用合适的配对比较，不把多个训练 seed 的重复场景当作完全独立样本夸大精度。
- 随机抽样的成功/失败视频，以及带物体位置、夹爪状态、技能切换和终止原因的轨迹记录。

至少回答四个问题：

1. DP 的 ID 失败是否来自已修复的实现错误？如果不是，剩余证据支持什么原因？
2. M1 相比 M0 的变化有多少来自分段/层级结构？
3. M2 相比 M1，在哪种 OOD 有 prior 增益，是否损害 ID？
4. API 实际提出了什么 prior，通过什么代码实现，改变了哪个行为？

不要把“神经网络训练成功”“API 调用成功”“子技能返回 completed”“整段任务成功”混为一谈。

## 11. 最终交付与停止条件

完成后提供以下内容；尽量集中到 Exp2 目录，避免制造大量散落报告：

1. 整理后的代码及根 README，说明 Exp1 和 Exp2 的唯一入口。
2. `experiments/exp2/MIGRATION.md`：路径迁移和归档说明。
3. `experiments/exp2/PROTOCOL.md`：方法、数据划分、预算、ID/OOD、成功条件、一次修订规则。
4. `experiments/exp2/DP_DIAGNOSIS.md`：DP 问题定位、真实修复和 ID 闭环证据。
5. `experiments/exp2/REPORT.md`：主要表格、prior 实现、结论与局限；由集中保存的 CSV/JSON 生成结果数字。
6. 一个统一的运行目录，保存必要 checkpoint、候选代码、配置、API 来源与成本、结果、少量视频及 incident 索引。

验收清单：

- [ ] Exp1 已独立保存，其已有结果和可复现性未被 Exp2 重构破坏。
- [ ] Exp2 没有多套互相冲突的有效代码或硬编码旧路径，常用命令已运行验证。
- [ ] 数据、控制、原生示范回放及评估器通过必要检查。
- [ ] API 可调用真实 GPU 训练；至少一个实际 API 候选完成非零梯度更新、保存、重载和闭环执行。
- [ ] API 可修改表示、loss、结构和 prior；主实验遵守同 backbone 边界。
- [ ] DP 已完成逐级诊断，ID 能力达到预定可用标准，或明确交付尚未解决的阻碍，未冒充已完成正式验证。
- [ ] M0/M1/M2 的关键混杂因素和计算差异有清晰说明。
- [ ] ID/OOD 在正式评估前已定义，测试结果没有用于挑 checkpoint 或定向清除失败。
- [ ] MuJoCo 反馈可信度有实际证据；正式 API 最多一次反馈修订。
- [ ] 无效运行已清理，策略真实失败被保留，正式结果只包含有效版本。
- [ ] 最终报告给出实际结论，既允许 DP 在 OOD 成功，也允许 APPL 没有提升。

开始执行时，先简要报告当前入口混乱在哪里和你采用的目录安排，然后继续做。过程中报告关键根因、修复和下一道检查即可，不要用大量工具计数代替研究进展。结束时明确写出：代码改好了什么、DP ID 到什么水平、APPL 的 prior 实际做了什么、哪些 OOD 有效、哪些没有，以及还缺哪一项。
